# dsh-pipeline bundle patch：profile 挂载后插入 pipeline 插件行
# 行 name 引用包名（dsh-pipeline），loader 按包 main（lib/index.js）解析 host 半；
# client 半由 dsh.client 声明经 client-modules 生成（/plugins/pipeline/client.js）
- insert:
    - id: pipeline
      name: dsh-pipeline
